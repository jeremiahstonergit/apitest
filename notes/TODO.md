# TODO

## Near-term

- Add smoke tests for a small set of read-only methods with dedicated test credentials
- Expand coverage beyond current included methods if/when new upstream snapshots are available
- Add optional `--strict` validation mode for cleaner curated examples
- Consider publishing MCP server runtime as installable package/container

## Nice-to-have

- Add diff report for request schema changes vs response schema changes separately
- Add namespace-specific rebuild/report commands if coverage becomes very large
