# Claude Code tuning

This project uses `CLAUDE.md` as the primary instruction source.

`settings.json` is intentionally omitted until a validated Claude Code settings schema is finalized for this repository.
