"""Application package for the SpaceWeb control plane.

Keep this package initializer side-effect free: serverless runtimes often mount
application files read-only, so task catalogs must be loaded in memory instead
of being merged into repository files during import.
"""
